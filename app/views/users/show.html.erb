	<% if @user.owned_spots %>


	<%# render partial: "spots/switch", locals: {user: @user, spots: @spots} %>
		<div class="owner">
			<%= render 'owner_show' %>
		</div>
		<div class="owner_wants_to_rent">
			<%= render 'renters' %>
		</div>
	<% else %>
	<div>
		<%= render 'renters' %>
	</div>
	<% end %>



<%# render partial: "spots/switch", locals: {user: @user, spots: @spots} %>
<%= render 'owner_show' %>

<%= button_to "Create New Spot", new_spot_path, method: :get %>

