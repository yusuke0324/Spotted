<% if @parking_spots.length != 0 %>
	<% @parking_spots.each do |spot| %>
  	<div class="col-sm-4" style="max-width:100%">
      <a href="/spots/<%= spot.id %>">
        <img class="steep" src="<%= spot.picture.url %>" alt="Lights" style="max-width:100%;max-height:165px;display:block;margin:0 auto">
      </a>
    	<div class="caption">
      <em><p>End Time: <%= spot.end_time.strftime("%m/%d/%Y %l:%M %p") %></p></em>
       <em>
       	<p>$<%= spot.price%>/h</p>
       </em>
       <em>
	       <p><%= spot.address%></p>
       </em>
      </div>
    </div>
	<% end %>
<% end %>